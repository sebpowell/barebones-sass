include _inc/_head.jade
include _inc/_layout.jade

.iframe-container

	.main-container

		h1.page-title Settings

		article.article.introduction

			p Barebones expects you to store pyour roject settings in a SASS map (if you're not familar with SASS maps, check out <a href="https://viget.com/extend/sass-maps-are-awesome" target="_blank">this article</a>).

			p These values are used by the various helpers and mixins included in the project. For example, the <span class="var">$breakpoints</span> map is used by the <span class="var">respond-above()</span> mixin, and the <span class="var">$colours</span> map is used by the text-color helper.

			p This map is not included in the release to give you the flexibility to specify your own settings.

			p You can download an example map <a href="https://github.com/sebpowell/barebones/blob/master/docs/css/_settings.scss" target="_blank">here</a>.


		article.article

			h2 Library Settings
			p Allows you to specify class prefixing (by default Barebones classes aren't namespaced).
			pre
				code.language-scss.
					$library: (
						prefix: "bb-",
					),

		article.article

			h2 Colours

			pre
				code.language-scss
					|   $colours: (
					|     brand: (
					|       primary: #157AFB,
					|       secondary: #FE5607,
					|       ...
					|     ),
					|     secondary: (
					|       orange: #F8B000,
					|       ...
					|     ),
					|     greyscale: (
					|       50: #FAFAFA,
					|       100: #F5F5F5,
					|       ...
					|     ),
					|     keyline: (
					|       light: #E5E5E5,
					|       ...
					|     ),
					|   )

			pre
				code.language-scss.
					.element {
					  background-color: colours($category, $value);
					}

		article.article
			h2 Fonts

			p Specify font familys' and their respective weights.

			pre
				code.language-scss

					|   fonts: (
					|     primary: (
					|       family: #{"Open Sans", Helvetica, sans-serif},
					|       weights: (
					|         light: 100,
					|         regular: 200,
					|         semi-bold: 500
					|       )
					|     ),
					|     secondary: (
					|       family: #{"Futura PT", Helvetica, sans-serif},
					|       weights: (
					|         bold: 600,
					|       )
					|     ),

			Access using <span class="function">fonts($family)</span> and <span class="function">weights($family, $weight)</span> functions.

		article.article
			h2 Grid

			p Specify global grid settings. Access using <span class="function">settings(layout, grid, $setting)</span> or <span class="function">grid($setting)</span>.

			pre
				code.language-scss
					|    grid: (
					|     columns: 12,
					|     site-width: 1200px,
					|     gutter: 1/4,
					|     gutter-type: margin,
					|    ),

		article.article
			h2 Breakpoints
			p Specify global breakpoints. It's generally best to set these as device sizes, rather than target specific devices.


			pre
				code.language-scss
					|    breakpoints: (
					|     device-small: 320px,
					|     device-medium: 480px,
					|     ...
					|    ),


		article.article
			h2 Sizes
			p Used to keep spacing consistent throughout the project. Access using <span class="function">settings(layout, sizes, $size)</span> or <span class="function">sizes($size)</span>.

			pre
				code.language-scss
					|    sizes: (
					|     quarter: 4px,
					|     half: 8px,
					|     base: 16px,
					|     ...
					|    ),

		article.article
			h2 Layers
			p
				| Stores z-indexes for different components so you can easily see the stacking order. Access using <span class="function">settings(layout, layers, $component)</span>  or <span class="function">layers($component)</span>.
			pre
				code.language-scss
					|    layers: (
					|     modal: 5,
					|     backdrop: 2,
					|     navigation: 4,
					|     ...
					|    ),




